<h1>Edit item</h1>

<%= form_for @item do |f| %>
	<p>
		<%= f.label :time %><br />
		<%= f.text_field :time %>
	</p>
	<p>
		<%= f.label :content %><br />
		<%= f.text_area :content %>
	</p>
	<p>
		<%= f.submit "Update item" %>
	</p>
<% end %>